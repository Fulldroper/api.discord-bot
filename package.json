{
  "name": "bot-client",
  "version": "1.0.0",
  "description": "",
  "github": "https://github.com/Fulldroper/bot-client.git",
  "main": "index.js",
  "scripts": {
    "test": "nodemon .",
    "deploy": "git add * && git commit -m deploy && git push %npm_package_github% master",
    "test:deploy": "nodemon --exec npm run deploy && node ."
  },
  "config": {
    "path": {
      "events": "events",
      "routes": "routes",
      "home": "www"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "discord.js": "^14.7.1",
    "fastify": "^4.10.2"
  }
}
